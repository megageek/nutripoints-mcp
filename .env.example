# Copy to .env for local development.
# Base URL of the Nutri Points instance this MCP server talks to.
NUTRIPOINTS_BASE_URL=http://localhost:8000
# A scoped API key created in Nutri Points Settings (Account & Security -> API keys).
NUTRIPOINTS_API_KEY=
